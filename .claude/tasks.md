# Tasks

Dan annotates an item directly with a line beginning `DAN:` underneath it —
that annotation is an instruction, work the item the way he answered it
there, not as commentary to re-litigate.

## Next-version release — shipped as 0.5.2 (2026-08-20)

Full plan was at `docs/internal/plan.md` (gitignored). All items landed;
see `docs/changelog.md`'s `[0.5.2]` entry for the public writeup and
`.claude/sessions.md`'s 2026-08-20 entry for how it was executed.

Nothing currently pending.
