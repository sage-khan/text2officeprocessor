# Optional cloud API keys (used only when the corresponding provider is selected)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=
GROQ_API_KEY=

# Optional key for protected vLLM endpoints (default for open local server is "EMPTY")
VLLM_API_KEY=EMPTY
